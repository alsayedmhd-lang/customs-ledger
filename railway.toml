[build]
builder = "DOCKERFILE"
dockerfilePath = "@workspace/api-server/Dockerfile"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
